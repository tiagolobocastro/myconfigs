apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-$N
  namespace: ${NS:-puls8}
spec:
  accessModes:
    - ReadWriteOnce
    #- ReadWriteMany
  volumeMode: Block
  resources:
    requests:
      storage: $SZ
  storageClassName: $SC

